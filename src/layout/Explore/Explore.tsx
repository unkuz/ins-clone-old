import { withLayout } from '@/hoc/layout/withLayout';
import React from 'react';

const Explore: React.FC = () => {
  return <div className="w-full min-h-screen bg-black/20"></div>;
};

export default withLayout(Explore);
